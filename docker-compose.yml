version: '3.8'

services:
  livros-ia:
    build: .
    ports:
      - "3000:3000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - NODE_ENV=production
    volumes:
      - ./backend/outputs:/app/backend/outputs
    restart: unless-stopped
